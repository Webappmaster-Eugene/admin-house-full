import { RegisterView } from '@/widgets/auth';

export const metadata = {
  title: 'House Admin: регистрация',
  description: 'Register page',
};

export default function RegisterPage() {
  return <RegisterView />;
}
